<mvc:View  controllerName="alfa03.sapui5.controller.HelloPanel"
           xmlns="sap.m" 
           xmlns:mvc="sap.ui.core.mvc"
           xmlns:core="sap.ui.core"
           displayBlock="true">


     <Panel headerText="{i18n>helloPanel}" class="sapUiResponsiveMargin" width="auto">
            <content>

                 <Button text="{i18n>helloDialogBut}" 
                        icon="sap-icon://action"
                        press=".onOpenDialog" 
                        class="sapUiSmallMarginEnd"/>
                               
                <Button text="{i18n>showHelloButtonText}" 
                        press=".onShowHello" 
                        class="sapUiSmallMarginEnd"/>
                                
                <Input value="{/recipient/name}"
                        valueLiveUpdate="true"
                        width="60%"/> 

                <Text text="Hello {/recipient/name}"
                    class="sapUiSmallMargin boldElments sapthemeHighlight-asColor"/>

            </content>
    </Panel>

    <core:Fragment fragmentName="alfa03.sapui5.view.Form01" type="XML"/> 
</mvc:View>